BEGIN
  EXECUTE IMMEDIATE 'CREATE TABLE balances (
    id INTEGER GENERATED ALWAYS AS IDENTITY PRIMARY KEY,
    amount INTEGER NOT NULL
  )';

  EXECUTE IMMEDIATE 'INSERT INTO balances (amount) VALUES (100)';
END;
